import React from 'react';
import { expect, renderComponent } from 'chai';
import Week from '../containers/dashboard-content/week';


describe('Week', () => {
	it('shoul do something', () => {
		const wrapper = mount(MainComponent);
		expect(Week.prototype).to.not.be.null;
	})
})